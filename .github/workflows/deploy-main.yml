name: Deploy Main Branch

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  deploy:
    uses: siteforward-ai/workflows/.github/workflows/cloudflare-pages-deploy.yml@main
    with:
      branch: main
    secrets: inherit
