name: Deploy Preview

on:
  push:
    branches:
      - '**'
      - '!main'

permissions:
  contents: read
  pull-requests: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      branch: ${{ github.ref_name }}
    secrets: inherit
