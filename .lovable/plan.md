## Scope

Only `src/components/sections/home/HomeHero.tsx`. One headline replacement, no other changes.

## Replacement

From:

```text
Every AI coding tool can read your source code.
AppMap shows it what actually happened.
```

To:

```text
Your AI reads the code.
AppMap shows what actually happened.
```

The line break after the first sentence stays. `Em` wraps only "actually happened" (magenta accent).

## Unchanged

Eyebrow, subheadline, install buttons, free/scale line, video, chips, and the closing caption.

## Verification

Typecheck, then confirm the hero renders the new headline with no em-dashes or banned terms.
