export function CatchPrevention() {
  return (
    <section className="bg-[#0d0a1a] px-6 py-14">
      <div className="mx-auto max-w-[1120px]">
        <h2 className="text-[24px] font-extrabold leading-tight tracking-[-0.7px] text-[#f2effb] sm:text-[30px]">
          Catch unexpected behavior while the change is still in the editor.
        </h2>
        <p className="mt-3 max-w-[720px] text-[16px] leading-relaxed text-[#a99fc7]">
          AppMap gives developers and AI agents runtime evidence as they work, so they can investigate calls, queries, exceptions, and side effects before the change moves downstream.
        </p>
      </div>
    </section>
  );
}
